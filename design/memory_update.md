メモリの更新は基本的にgpt-6-lunaに投げる。reasoningとかtool callとかは全部投げ捨てて、userとassistantだけを投げるようにして。投げるターン数は最大10件に制限して。
あと、更新時に全部出力するのやめて
メモリ更新のログを、reasoningとかtool callとかを投げるブロックの中に入れて、基本的にユーザーはメモリ更新なしか、ありかだけがわかるようにして。クリックすると今までのが見えるようにして。あと、同一セッションで複数メモリ更新がされる場合(複数回ターンがある場合)、各メモリ更新で少なくとも途中まではKVキャッシュって保存されるよね？reasoning noneなので
